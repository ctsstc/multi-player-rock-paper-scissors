{
  "name": "async-rps",
  "private": true,
  "version": "0.2.0",
  "description": "Async rock paper scissors for people who are never online at the same time.",
  "type": "module",
  "bin": {
    "async-rps": "bin/async-rps.mjs"
  },
  "files": [
    "bin",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "node dev/server.ts",
    "deploy": "wrangler deploy",
    "test": "node --test src/*.test.ts",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types",
    "cli": "node bin/async-rps.mjs"
  },
  "devDependencies": {
    "@hono/node-server": "^2.1.1",
    "@libsql/client": "^0.18.0",
    "hono": "^4.13.7",
    "typescript": "^7.0.2",
    "wrangler": "^4.131.0"
  }
}
