{
  "name": "async-rps",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "node dev/server.ts",
    "deploy": "wrangler deploy",
    "test": "node --test src/*.test.ts",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "@libsql/client": "^0.18.0",
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "@hono/node-server": "^2.1.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.131.0"
  }
}
